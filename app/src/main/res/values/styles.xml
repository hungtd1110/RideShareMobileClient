<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <!--<item name="colorPrimary">@color/colorPrimary</item>-->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <!--<item name="colorAccent">@color/colorAccent</item>-->
    </style>

    <style name="LoginTheme" parent="AppTheme">
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="DetailTheme" parent="AppTheme">
        <item name="colorPrimaryDark">@color/colorWhite</item>
    </style>

    <style name="BaseTextView" parent="@android:style/Widget.TextView" >
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="ItemNavigationTextView" parent="BaseTextView">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:background">@drawable/background_ripple_navigation</item>
        <item name="android:gravity">center</item>
        <item name="android:padding">@dimen/padding_small</item>
        <item name="android:textColor">@color/colorGrayLight</item>
    </style>

    <style name="ToolItemTripTextView" parent="BaseTextView">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_marginEnd">@dimen/margin_large</item>
        <item name="android:layout_weight">1</item>
        <item name="android:background">@drawable/background_border_green</item>
        <item name="android:drawablePadding">@dimen/padding_small</item>
        <item name="android:gravity">center</item>
        <item name="android:padding">@dimen/padding_small_normal</item>
        <item name="android:textColor">@color/colorWhite</item>
        <item name="android:textSize">@dimen/font_normal</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>
